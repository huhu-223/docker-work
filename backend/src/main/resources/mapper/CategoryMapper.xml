<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.CategoryMapper">
    <select id="selectAll" resultType="com.test.entity.Category">
        SELECT * FROM category ORDER BY sort_order
    </select>

    <select id="selectById" resultType="com.test.entity.Category">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, description, sort_order)
        VALUES (#{name}, #{description}, #{sortOrder})
    </insert>

    <update id="update">
        UPDATE category SET name = #{name}, description = #{description}, sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <select id="countProducts" resultType="int">
        SELECT COUNT(*) FROM product WHERE category_id = #{categoryId}
    </select>
</mapper>
