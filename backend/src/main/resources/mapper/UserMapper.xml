<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.UserMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, phone, email, role)
        VALUES (#{username}, #{password}, #{phone}, #{email}, #{role})
    </insert>

    <select id="selectByUsername" resultType="com.test.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="selectById" resultType="com.test.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
</mapper>
