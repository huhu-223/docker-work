<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.OrderItemMapper">
    <insert id="insertBatch">
        INSERT INTO order_item (order_id, product_id, product_name, product_price, quantity, subtotal) VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productPrice}, #{item.quantity}, #{item.subtotal})
        </foreach>
    </insert>

    <select id="selectByOrderId" resultType="com.test.entity.OrderItem">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>
</mapper>
